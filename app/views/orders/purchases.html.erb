<div class="center">
  <h2>Your Purchases History</h2>
</div>

<table class="table table-striped table-bordered">
  <tr>
    <th class="center">Image</th>
    <th class="center">Item</th>
    <th class="center">Price</th>
    <th class="center">Seller</th>
    <th class="center">Date Purchaes</th>
  </tr>

  <% @orders.each do |order| %>
    <tr>
      <td calss="center"><%= image_tag order.listing.image.url(:thumb) %></td>
      <td calss="center"><%= order.listing.name %></td>
      <td calss="center"><%= number_to_currency(order.listing.price )%></td>
      <td calss="center"><%= order.seller.name %></td>
      <td calss="center"><%= order.created_at.strftime("%B %-d, %Y") %></td>
    </tr>
  <% end %>
</table>

<br>
<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>